# 运行期状态（写在 <repo> 或 DSH 配置目录里，不该进仓库）
theme-flag.json
quota-samples.json

# 壁纸视频：仓库里那份（assets/wallpaper.mp4，约 20MB）是**要提交**的，
# 所以这里不忽略它。如果你换成超大的原片，自己加一行忽略、或改用 Git LFS / Release 附件。

# 台词库（用 %APPDATA%\dsh-desktop\mornye-theme\lines.txt 那份，不进仓库）
lines.txt

# Node
node_modules/
npm-debug.log*
pnpm-debug.log*

# 编辑器 / 系统
.vscode/
.idea/
.DS_Store
Thumbs.db

# 本地密钥与配置（千万别提交）
.env
.env.*
*.local
